import { StyleSheet, Dimensions } from 'react-native'

const style = StyleSheet.create({
    flexbox: {
        display: flex,
        flexWrap: nowrap
    },
    textright: {
        alignContent: 'end',
    }
      
})

export default style