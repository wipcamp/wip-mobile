import { StyleSheet, Dimensions } from 'react-native'

const style = StyleSheet.create({
    liststyles = {
        titleStyle: {
          fontSize: 18,
          paddingLeft: 15
        }
      }
})

export default style